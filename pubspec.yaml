name: geolocator_linux
description: Geolocation Linux plugin for Flutter. This plugin provides the
  Linux implementation for the geolocator.
repository: https://github.com/baseflow/flutter-geolocator/tree/main/geolocator_linux
issue_tracker: https://github.com/baseflow/flutter-geolocator/issues?q=is%3Aissue+is%3Aopen
version: 0.2.4
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

flutter:
  plugin:
    implements: geolocator
    platforms:
      linux:
        dartPluginClass: GeolocatorLinux

dependencies:
  dbus:
    git: https://github.com/kumsumit/dbus.dart.git
  flutter:
    sdk: flutter
  geoclue:
    git: https://github.com/kumsumit/geoclue.dart.git
  geolocator_platform_interface:
    git: https://github.com/kumsumit/geolocator_platform_interface.git
  gsettings:
    git: https://github.com/kumsumit/gsettings.dart.git
  package_info_plus:
    git: https://github.com/kumsumit/package_info_plus.git

dev_dependencies:
  build_runner: ^2.15.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.7.0
  plugin_platform_interface:
    git: https://github.com/kumsumit/plugin_platform_interface.git
